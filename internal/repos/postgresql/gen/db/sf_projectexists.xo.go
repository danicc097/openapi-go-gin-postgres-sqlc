
// Code generated by xo. DO NOT EDIT.

//lint:ignore

package db

import (
	"context"
	"database/sql"
	"database/sql/driver"
	"encoding/csv"
	"errors"
	"fmt"
	"io"
	"io/ioutil"
	"regexp"
	"strings"
	"time"
	"github.com/lib/pq"
	"github.com/lib/pq/hstore"

	"github.com/google/uuid"

)
// ProjectExists calls the stored function 'public.project_exists(text) boolean' on db.
func ProjectExists(ctx context.Context, db DB, projectName string) (bool, error) {
	// call public.project_exists
	sqlstr := `SELECT * FROM public.project_exists($1) `
	// run
	var r0 bool
	// logf(sqlstr, projectName)
	if err := db.QueryRow(ctx, sqlstr, projectName).Scan(&r0); err != nil {
		return false, logerror(err)
	}
	return r0, nil
}


