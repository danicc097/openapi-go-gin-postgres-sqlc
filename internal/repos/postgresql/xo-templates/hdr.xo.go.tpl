{{ define "header" }}
{{- $tags := tags -}}
{{- $inject := inject -}}
{{- if $tags -}}
//go:build{{ range $tags }} {{ . }}{{ end }}

{{ end -}}
{{/* must be the first line, else golangci-lint fixes it */}}
// Code generated by xo. DO NOT EDIT.

//lint:ignore

package {{ pkg }}

import (
	"context"
	"database/sql"
	"database/sql/driver"
	"encoding/csv"
	"errors"
	"fmt"
	"io"
	"io/ioutil"
	"regexp"
	"strings"
	"time"

  {{/* only models allowed to prevent circular deps */}}
	models "github.com/danicc097/openapi-go-gin-postgres-sqlc/internal/models"

{{- if driver "postgres" }}
	"github.com/lib/pq"
	"github.com/lib/pq/hstore"
{{ end }}{{ range imports }}
	{{ with .Alias }}{{ . }} {{ end }}{{ .Pkg }}
{{ end }}
)

{{- if $inject }}
{{ $inject }}
{{- end }}
{{ end }}
