package services

// TODO only use services, not repos. No need to turn redis, etc. into repo for now.
// TODO service interfaces

type Default struct {
}
type Docs struct {
}
type Fake struct {
}
type Pet struct {
}
type Store struct {
}
type User struct {
}
